# Hotel chatbot client — LLM + MCP stdio + web chat.
# Python 3.10+. Install: python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
anthropic>=0.40.0
mcp>=1.28.0,<2
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
pymongo>=4.10.0
dnspython>=2.7.0
httpx>=0.27.0
